apiVersion: eaas.envmgmt.io/v1
kind: Environment
metadata:
  name: optouttest
  project: defaultproject
spec:
  agents:
  - name: eksagent
  - name: eksagent2
  template:
    name: optouttest
    version: v5
    versionState: unspecified
  variables:
  - name: username
    options:
      override:
        type: allowed
      required: true
    value: vijays
    valueType: text
